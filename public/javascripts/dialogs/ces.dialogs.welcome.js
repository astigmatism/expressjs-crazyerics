var cesDialogsWelcome = (function($el, $wrapper, args) {

    this.OnOpen = function(args) {
    };

    this.OnClose = function(callback) {
        return callback();
    };

    this.OnIntroAnimationComplete = function() {

    };

    var Constructor = (function() {
    })();
});